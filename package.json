{
  "dependencies": {
    "aws-sdk": "^2.1174.0",
    "busboy": "^1.6.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "serverless-offline": "^9.0.0"
  },
  "scripts": {
    "start": "nodemon --exec sls offline",
    "test": "jest --verbose --runInBand"
  }
}
